<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:hz="http://www.hazelcast.com/schema/spring"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.hazelcast.com/schema/spring http://www.hazelcast.com/schema/spring/hazelcast-spring.xsd
                            http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee.xsd">


    <!-- имя очереди сервера, куда клиент отправляет запросы-->
    <jee:jndi-lookup id="serverQueueName" jndi-name="java:comp/env/server.queue.name"/>
    <!-- имя очереди клиента, куда сервер отправляет ответы-->
    <jee:jndi-lookup id="clientQueueName" jndi-name="java:comp/env/client.queue.name"/>

    <!-- имя очереди сервера, куда клиент отправляет запросы-->
    <!--
    <bean id="serverQueueName" class="java.lang.String">
        <constructor-arg type="java.lang.String" value="server-queue-app"/>
    </bean>
    -->

    <!-- имя очереди клиента, куда сервер отправляет ответы-->
    <!--
    <bean id="clientQueueName" class="java.lang.String">
        <constructor-arg type="java.lang.String" value="client-queue-app-1"/>
    </bean>
    <-->

    <hz:hazelcast id="hazelcastInstance">
        <hz:config>
            <hz:queue name="client-queue-app*"/>
            <!--<hz:idGenerator name="client-server-id-generator"/>-->
        </hz:config>
    </hz:hazelcast>

    <!-- сервис для работы с распределенными объектами-->
    <bean id="dosService" class="com.payway.messaging.core.service.hz.HazelcastDistributedObjectServiceImpl" depends-on="hazelcastInstance">
        <property name="hazelcastInstance" ref="hazelcastInstance"/>
    </bean>

    <!-- сюда уходят запросы-->
    <bean id="serverQueue" class="java.util.concurrent.BlockingQueue" factory-bean="dosService" factory-method="createQueueByName" depends-on="dosService">
        <constructor-arg value="#{serverQueueName}"/>
    </bean>

    <!-- отсюда приходят ответы-->
    <bean id="clientQueue" class="java.util.concurrent.BlockingQueue" factory-bean="dosService" factory-method="createQueueByName" depends-on="dosService">
        <constructor-arg value="#{clientQueueName}"/>
    </bean>

</beans>